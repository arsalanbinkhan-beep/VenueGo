<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VenueGo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.VenueGo" parent="Base.Theme.VenueGo" />
    <style name="RatingBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorControlNormal">@color/light_gray_text</item>
        <item name="colorControlActivated">@color/yellow_star</item>
    </style>

</resources>